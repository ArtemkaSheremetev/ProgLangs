// Тестовая программа для языка Hello
method main() : int
var x, y : int;
var flag : bool;
begin
    x := 10;
    y := 20;
    flag := true;

    if x < y then
        popa();
    else
        jopa();

    repeat
        y := y - 1;
    until y == 0;
end;

method popa() : int
var x, y : int;
var flag : bool;
begin
    x := 10;
    y := 20;
    flag := true;

    if x < y then
        x := x + y;
    else
        x := x * y - 3 + 4;
        
    repeat
        y := y - 1;
    until y == 0;
end;

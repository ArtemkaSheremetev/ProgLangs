// Тестовая программа для языка Hello. Исправленная версия
method main() : int
var x, y : int;
var flag : bool;
begin
    x := 10;
    y := 20;
    flag := true;

    if x < y then
        popa();
    else
        banana();
    popa();
    repeat
        y := y - 1 * 2 / x;
        x := 1 + 3 + 8 / 2 - y;
    until y == 0;
end;